var rr = new FileReader();
rr.onload = async function (res)
{
    state.arrayBuffer = res.currentTarget.result;
    await ReadArrayBufferAsync(state);
    ProcessResponse(state);
};
rr.readAsArrayBuffer(state.blob);

function func1(...names)
{
    if (spinner1.Remove(names), wclient.xhr.status < 200 || wclient.xhr.status >= 300)
    {
        let title1 = wclient.xhr.status + " " + "gasdfa";
    }

	for (let m1 of [...names])
    {
        let b1 = m1 + 2;
    }

	while (names[0] > 0)
    {
        let y1 = Date.now();
    }

	let fn2 = (...ages) =>
    {
        let mk33 = ages[0] + "dva";
	}
}

console.log(func1());


